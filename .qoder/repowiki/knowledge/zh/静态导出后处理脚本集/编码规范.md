- 脚本入口统一以同步 `main()` 函数组织逻辑，失败时通过 `console.error` 打印原因并 `process.exit(1)` 退出。
- 通过 `fileURLToPath(import.meta.url)` + `path.dirname` 推导 `__dirname`，再基于相对路径定位 `../out` 或同级配置文件，避免硬编码绝对路径。
- 对 `out/` 目录的存在性做前置检查，缺失时给出“先运行 next build”的提示再退出，保证脚本幂等且可安全重复执行。